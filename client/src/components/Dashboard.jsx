import React, { useContext } from 'react';
import Logout from './Logout';

const Dashboard = () => {
  return (
    <>
      <h1>Dashboard</h1>
      <Logout />
    </>
  );
};

export default Dashboard;
